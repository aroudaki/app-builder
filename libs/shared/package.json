{
    "name": "@ai-app-generator/shared",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc",
        "type-check": "tsc --noEmit",
        "test": "npm run build && node dist/run-test.js"
    },
    "dependencies": {
        "events": "^3.3.0"
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "typescript": "^5.3.0"
    }
}